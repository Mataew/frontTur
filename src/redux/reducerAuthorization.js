

const initialState = {
  signingUp: false,
  error: null,
};

export default function authorizatonReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}


